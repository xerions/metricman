###########################################################################
## Metrics - SECTION

# Influxdb configuration in form of <user>:<password>@<host>[:<port>]/<database>
metrics.influx.db = http://127.0.0.1:8086/default_db
# Influxdb additional tags for each metric in form of <key>:<value>,...
metrics.influx.tags = node:node_name,region:de
# Set window size in ms for batch sending.
# This means reported will collect measurements within this interval and send all measurements in one packet.
metrics.influx.batch_window_size = 0
